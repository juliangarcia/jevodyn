package de.mpg.jevodyn.stochastic;

import junit.framework.TestCase;

import org.junit.Test;

public class SimulationTimeSeriesTest extends TestCase {

	@Test
	public void testSimulateTimeSeries() {
		// int[] array = { 1, 20 };
		// //int popSize = 10;
		// Long seed = System.currentTimeMillis();
		// int numberOfTimeSteps = 1000;
		// int reportEveryTimeSteps = 1;
		// SimplePopulationImpl startingPopulation = new SimplePopulationImpl(
		// array);
		// boolean includeTotalPayoff = false;
		// String fileName = "pepito2.txt";
		// MoranProcess mp = new MoranProcess(new SimplePopulationImpl(array),
		// new GamePayoffCalculator(Games.prionersDilemma()), 0.5, 1.0);
		// SimulationTimeSeries series = new SimulationTimeSeries(mp);
		// try {
		// series.simulateTimeSeries(numberOfTimeSteps, reportEveryTimeSteps,
		// startingPopulation, seed, includeTotalPayoff, fileName);
		// } catch (IOException e) {
		// e.printStackTrace();
		// assertTrue(false);
		// }
		assertTrue(true);
	}

}
